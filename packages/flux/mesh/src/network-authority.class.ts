/*

import {
    RPCClient,
} from '@flux/shared';

export class AuthorityClient extends RPCClient<'authorize'> {
    public authorize(
        jwt: unknown
    ): Promise<boolean> {
        return super.call('authorize', jwt);
    }
}

    */
