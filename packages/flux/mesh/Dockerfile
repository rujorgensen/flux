# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build flux-mesh`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t flux-mesh`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system flux-mesh && \
          adduser --system -G flux-mesh flux-mesh

COPY dist/apps/flux/backend-data-lake flux-mesh/
RUN chown -R flux-mesh:flux-mesh .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix flux-mesh --omit=dev -f install

CMD [ "node", "flux-mesh" ]
