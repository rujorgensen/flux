{
    "name": "persistica-flux-authority",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "packages/flux/authority/src",
    "projectType": "library",
    "tags": [
        "type:npm-package",
        "scope:any"
    ],
    "targets": {
        "build": {
            "executor": "@nx/esbuild:esbuild",
            "outputs": [
                "{options.outputPath}"
            ],
            "options": {
                "outputPath": "dist/packages/flux/authority",
                "main": "packages/flux/authority/src/index.ts",
                "tsConfig": "packages/flux/authority/tsconfig.lib.json",
                "assets": [],
                "format": [
                    "cjs"
                ],
                "generatePackageJson": true
            }
        },
        "bun-serve": {
            "executor": "nx:run-commands",
            "options": {
                "command": "bun --watch run ./apps/flux/authority/src/lib/flux.ts"
            }
        },
        "madge-graph": {
            "executor": "nx:run-commands",
            "options": {
                "command": "npx madge --image graph.svg --extensions ts,tsx ./apps/flux/authority/src"
            }
        },
        "demo": {
            "executor": "nx:run-commands",
            "options": {
                "command": "bun --watch run ./apps/flux/authority/src/demo/http.server.ts"
            }
        },
        "nx-release-publish": {
            "options": {
                "packageRoot": "dist/{projectRoot}"
            }
        }
    },
    "release": {
        "version": {
            "currentVersionResolver": "git-tag",
            "fallbackCurrentVersionResolver": "disk",
            "preserveLocalDependencyProtocols": false,
            "manifestRootsToUpdate": [
                "dist/{projectRoot}"
            ]
        }
    }
}