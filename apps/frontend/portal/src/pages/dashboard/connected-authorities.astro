---
import DashboardLayout from "$lib/layouts/dashboard/DashboardLayout.astro";

// * Dashboard Tables
import SvelteConnectedAuthorities from "$lib/components/dashboard/tables/SvelteConnectedAuthorities.svelte";

const networkId: string = "rAnD0M-demo-network-id";
---

<DashboardLayout>
    <SvelteConnectedAuthorities client:load networkId={networkId} />
</DashboardLayout>
