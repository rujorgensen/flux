---
import BaseHead from "$lib/components/BaseHead.astro";
import { SITE_TITLE, SITE_DESCRIPTION } from "$lib/consts";
import { ConnectedAgentsPageComponent } from "../../components/angular/pages/connected-agents/connected-agents-page.component";

import { auth } from "@backend/portal/auth";

const networkId: string = "rAnD0M-demo-network-id";

const session = await auth.api.getSession({
    headers: Astro.request.headers,
});

if (!session) {
    return Astro.redirect("/sign-in");
}

const userSession = session.user;
---

<!doctype html>
<html lang="en">
    <head>
        <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
    </head>
    <body>
        <ConnectedAgentsPageComponent
            client:load
            networkId={networkId}
            userSession={userSession}
            activeNetworkName="Acme Inc"
        />
    </body>
</html>
